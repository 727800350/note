#!/bin/bash
## my compile in docker dev
set -x

if [ $# -lt 1 ]
then
	echo "arg error" >&2
	exit 1
else
	code="$1"
fi

shift 1

g++ -std=c++11 -g -Wall $code -I ~/git/wcg-api/output/include -L ~/git/wcg-api/output/lib -lwcg-api -lglog -lgflags $@ -lpthread

exit 0

